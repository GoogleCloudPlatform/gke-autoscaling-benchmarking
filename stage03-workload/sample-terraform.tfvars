# By default, the workload will run on any node in the cluster. You can
# restrict it to only certain node pools by setting this variable.
#node_selector = {
#  "cloud.google.com/gke-nodepool" = "e2-nodepool"
#}
